
  $(document).ready(function() {
    var client = algoliasearch("J2ELQ5BG86", "a8ceec80c94969b38c848b92effd9263");
    var template = Hogan.compile('<img src="http://fakeimg.pl/100x100" /><h3>{{{_highlightResult.name.value}}}</h3>{{{_highlightResult.description.value}}}');
    $('input#search-field').typeahead(null, {
      source: client.initIndex('Newsletter').ttAdapter(),
      displayKey: 'name',
      templates: {
        suggestion: function(hit) {
          return template.render(hit);
        }
      }
    });
  });

