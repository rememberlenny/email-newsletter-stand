- item = item ? item : local[:item]
- @emails = Email.get_recent(item.id, 5)
%h3
  = link_to item[:name], item
  - @emails.each do |email|
    %p
      = link_to email.subject + ', ' + time_ago_in_words(email.created_at), email
